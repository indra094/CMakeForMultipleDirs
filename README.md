# CMakeForMultipleDirs
main calls funcs from classes H1 in Folder1 and H2 in Folder2

eclipse->open projects from filesystem->import path of the superproject

See branch master. That's where the files are.
